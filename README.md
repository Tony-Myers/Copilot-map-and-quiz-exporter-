# Study Tool Launcher v3

This version keeps the original quiz and mind map apps intact and uses a launcher page to store a Copilot JSON payload in browser localStorage.

## Workflow
1. Open `index.html`
2. Paste the Copilot JSON payload and click **Save payload**
3. Click **Open Quiz Tool** or **Open Mind Map Tool**
4. The original app loads the payload automatically
